// Создайте переменную `c` и запишите в нее любое число. Если `c` больше 0 и меньше 10, то выведите в консоль "Верно", иначе выведите "Неверно".
// Проверьте работу скрипта при c равном 0, 10, −3, 2.
// **Для решения задачи используйте логический оператор && (логическое И).*

let c = 2;

if(c > 0 && c < 10){
    console.log('Верно');
} else {
    console.log('Неверно');
}